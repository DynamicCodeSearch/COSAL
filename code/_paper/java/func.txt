Integer[] func(int x) {
    int[] nums = IntStream.range(0, x).toArray();
    List<Integer> evens = new ArrayList<>();
    for (int i=0; i<nums.length(); i++)
        if (nums.get(i) % 2 == 0)
            evens.add(num.get(i));
    return evens.toArray();
}