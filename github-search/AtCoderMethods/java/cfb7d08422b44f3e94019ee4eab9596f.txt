private static void solve(ConsoleScanner cin, PrintWriter cout) {
    
    int n = cin.nextInt();
    now.getAsLong();
    String ans = solve(n);
    cout.println(ans);
    assert 0 == tr("elapsed", now.getAsLong());
}