public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
    int X = sc.nextInt();
    double x = 0;
    double y = 0;
    x = Math.sqrt(X);
    y = Math.sqrt(x);
    System.out.println((int) y);
}