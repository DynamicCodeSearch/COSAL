public static void solve(PrintWriter out, FastScanner in) {
    int H = in.nextInt();
    int W = in.nextInt();
    String[] c = in.nextStringArray(H);
    for (int i = 0; i < H; i++) {
        out.println(c[i]);
        out.println(c[i]);
    }
}