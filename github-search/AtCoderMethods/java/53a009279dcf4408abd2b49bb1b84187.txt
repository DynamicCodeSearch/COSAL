private void solve() {
    int a, b, c;
    a = in.nextInt();
    b = in.nextInt();
    c = in.nextInt();
    if ((c >= a) && (c <= b)) {
        out.print("Yes");
    } else {
        out.print("No");
    }
}