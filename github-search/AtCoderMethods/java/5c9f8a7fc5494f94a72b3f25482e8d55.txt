static void solve(FastScanner sc, PrintWriter out) {
    int H = sc.ni();
    int W = sc.ni();
    for (int i = 0; i < H; i++) {
        String line = sc.ns();
        out.println(line);
        out.println(line);
    }
}