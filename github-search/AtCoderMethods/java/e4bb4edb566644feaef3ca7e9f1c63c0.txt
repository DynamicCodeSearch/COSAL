public static void main(String[] args) {
    FS reader = new FS();
    int x1 = (int) reader.nextLong();
    int y1 = (int) reader.nextLong();
    int r = (int) reader.nextLong();
    int x2 = (int) reader.nextLong();
    int y2 = (int) reader.nextLong();
    int x3 = (int) reader.nextLong();
    int y3 = (int) reader.nextLong();
    solver(x1, y1, r, x2, y2, x3, y3);
}