public static void main(String[] args) {
    InputReader in = new InputReader(System.in);
    PrintWriter w = new PrintWriter(System.out);
    w.println(Math.max(in.nextLong() * in.nextLong(), in.nextLong() * in.nextLong()));
    w.close();
}