private static void solve() {
    int h = sc.nextInt();
    @SuppressWarnings("unused")
    int w = sc.nextInt();
    char[][] c = new char[h][];
    for (int i = 0; i < h; i++) {
        c[i] = sc.next().toCharArray();
        pr.println(c[i]);
        pr.println(c[i]);
    }
}