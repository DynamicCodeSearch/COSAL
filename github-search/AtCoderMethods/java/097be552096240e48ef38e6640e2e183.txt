private void solve() throws IOException {
    int R = nextInt();
    int C = nextInt();
    for (int r = 0; r < R; ++r) {
        String s = reader.readLine();
        writer.println(s);
        writer.println(s);
    }
}