def abcpop(lis):
    if lis[0] == "a":
        lis.pop(0)
        pre = a
    elif lis[0] == "b":
        lis.pop(0)
        pre = b
    elif lis[0] == "c":
        lis.pop(0)
        pre = c
    return pre